pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                /* CORRECT */
                echo 'hello world'
                sh('ls -al')
            }
        }
    }
}